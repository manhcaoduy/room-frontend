enum AuthEnum {
  accessTokenStorageKey = "nft-room-access-token",
  refreshTokenStorageKey = "nft-room-refresh-token",
}

export const ENUM = {
  auth: AuthEnum,
};

export enum WalletTypeEnum {
  EVM = 0,
}
